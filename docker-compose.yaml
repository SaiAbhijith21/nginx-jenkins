version: "3.8"

services:
  nginx-web:
    image: nginx:alpine
    container_name: nginx_static_site
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
    restart: unless-stopped